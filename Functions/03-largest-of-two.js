// 03: Create a function to return largest of two numbers:

function findMax(a,b){
    if(a>b){
        return (`${a} is the largest`);
    }
    else{
        return (`${b} is the largest`);
    }
}
console.log(findMax(34,5));